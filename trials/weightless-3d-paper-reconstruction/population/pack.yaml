apiVersion: starlings/v1
kind: EmergencePack

metadata:
  name: weightless-3d-paper-reconstruction
  version: 0.1.0

state:
  variables: variables.yaml
  invariants: invariants.yaml

population:
  operators: operators.yaml

targets:
  - asset.valid
