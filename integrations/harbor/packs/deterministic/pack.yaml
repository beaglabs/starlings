apiVersion: starlings/v1
kind: EmergencePack

metadata:
  name: hosted-harbor-deterministic
  version: 0.1.0

state:
  variables: variables.yaml
  invariants: invariants.yaml

population:
  operators: operators.yaml

policy:
  actions: policies/actions.yaml

targets:
  - final.b64
